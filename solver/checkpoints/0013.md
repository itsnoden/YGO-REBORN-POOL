# Checkpoint 0013 — 2026-09-07

Status: **2,273 / 2,273 effective Reborn cards remain mapped**, canonical duel verification is green with the two reviewed simulator corrections active, and the hash-bound errata gate is down to **144 unresolved lexical behavior rows** after **42 accepted reviews**. Canonical pilot remains uncertified for deck ranking. **No strongest deck has been established yet.**

## Verified simulator baseline

Solver Verify **#204** (`run_id=34182224605`, commit `694253a7801326daa984b14550692c82a9546ac0`) passed the complete canonical pipeline with both `Dimension Jar` and `Fushioh Richie` overrides active.

Hard state:
- effective pool: **2,273**
- mapped: **2,273 / 2,273**
- unmapped: **0**
- missing scripts: **0**
- `Double Summon` is absent; `reborn-0530` is `Dragged Down into the Grave`
- CardScripts pin: `14064037f42d0ae0bb9f8577d7eba2baad95e633`
- Reborn-affecting upstream script drift after pin: **0**

The canonical pilot is still only a pilot-skill gate, not deck-strength evidence.

## Current errata gate

Text Difference Audit **#38** (`run_id=34182948452`, commit `339444c3be7b7cb40ee98e346c381196ced96e0f`) passed:
- raw lexical effect rows: **186**
- accepted reviews: **42**
  - wording-equivalence: **32**
  - implementation-behavior: **10**
- stale/invalid reviews: **0**
- orphan reviews: **0**
- unresolved lexical behavior blockers: **144**

The latest implementation reviews complete the remaining simple legacy `select` vs current PSCT `target` pattern:
- `Invader of the Throne`
- `Remove Trap`

Both exact pinned scripts use `EFFECT_FLAG_CARD_TARGET` and `Duel.SelectTarget`; they are script-SHA-bound reviews, not text-similarity assumptions.

## Confirmed simulator corrections

1. `Dimension Jar` (`73414375`): upstream selection filter could allow non-Monster GY cards; Reborn override requires monsters while preserving Spirit Elimination-compatible handling. Bound override SHA-256: `60678ac2489585d9b4d2b94601ca16c7d50c1123469721235123dacb9be062a4`.
2. `Fushioh Richie` (`39711336`): upstream only used `EnableReviveLimit()`, allowing later generic revival after Great Dezard. Reborn override adds `AddCannotBeSpecialSummoned()` while Great Dezard's intended hand/Deck nocheck Summon remains legal. Bound override SHA-256: `8ffa8da98d282cf02f8cd12b00b946d7dcbfc38c5c27fd98d67ef5181508b0b3`.

## Deliberately unresolved / do not guess

- `Scroll of Bewitchment` — Attribute-choice nuance.
- `Mushroom Man #2` — official `loses 300 LP` vs Lua effect damage requires authoritative interaction verification.
- `Metalsilver Armor` — current `targets 1 monster` scope needs multi-target interaction verification.
- `Hero Barrier` — activation/timing semantics need interaction verification.
- `Prohibition` — broad `EFFECT_FORBIDDEN` implementation should be tested rather than certified from source reading alone.
- `Poison Draw Frog` — face-down battle-destruction exception needs a deterministic engine-state test; simple `previous position` code reading is insufficient.
- `Arcana Force 0 - The Fool` — position-change wording/rules nuance needs deeper verification.

## Resume

1. Preserve all hard pool/source corrections and the no-human-meta strategy doctrine.
2. Keep `Double Summon` absent from every candidate, simulation, probability model, and decklist.
3. Continue the **144 unresolved** behavior rows, prioritizing real executable/ruling mismatches.
4. Build deterministic interaction tests for the deliberately unresolved edge cases instead of inferring rulings.
5. Use narrow overrides + regression tests + exact script-SHA-bound implementation reviews for genuine simulator bugs.
6. Keep upstream script-drift checking mandatory.
7. Do not use the canonical pilot for deck ranking yet.
8. Add Extra Deck optimization only after errata and pilot gates strengthen.
9. Only then run certified payoff matrices and adversarial/double-oracle deck search.
10. Never claim a strongest deck prematurely.
