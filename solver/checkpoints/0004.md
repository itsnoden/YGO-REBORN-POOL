# Checkpoint 0004 — 2026-09-06/07

Status: complete-duel protocol/observation coverage is now live-verified under the
user-confirmed YGO Reborn rules profile, and the first zero-prior learning smoke
successfully completes full games and updates a policy. **No strongest deck has
been established yet.**

## Authoritative project constraints

Continue using only:

- exact 2,273-card YGO Reborn pool;
- YGO Reborn banlist;
- actual Yu-Gi-Oh rules, with the current solver profile now user-confirmed as
  matching YGO Reborn;
- latest official card text/errata **always**.

Do not use tournament decklists, historical decks, Reddit, tier lists, community
strategy priors, outside metagame data or archived human rankings to guide deck
search. Simulator engine/script/database data may implement rules/effects only.

## Rules profile now certified for this project

`reborn.rules.PROFILES['reborn']` is the default profile used by `Duel`:

- 8,000 LP
- opening hand 5
- draw 1 per normal draw
- no first-turn draw
- pinned-core MR5/current field-mechanics flags
- TCG SEGOC flags already used by the solver

User confirmed on 2026-09-06 that this profile matches YGO Reborn. This rules
confirmation does **not** change the separate latest-errata requirement.

## Protocol and prompt coverage added since checkpoint 0003

The strict protocol layer now handles the original decision families plus:

- tribute selection
- counter allocation
- select-sum/material selection
- sort chain/card
- select/unselect
- announce race
- announce attribute
- announce number
- rock-paper-scissors
- database-backed announce-card declarability using the pinned core opcode
  semantics and only Reborn-mapped passcodes

Unsupported/illegal responses remain hard blockers; nothing guesses around
unknown engine prompts.

## Privacy-safe observation/prompt boundary

Added public/private observation querying against the pinned ocgcore query ABI.
The policy path now separates:

1. privileged referee `Decision` objects/raw messages;
2. viewer-specific information-safe duel observations;
3. filtered policy prompts that remove hidden opponent card codes and unsafe
   referee-only helper fields.

The live probes assert this boundary on every decision. A learned policy must use
`observation.py` + `policy_view.py`; raw engine prompts are not policy input.

## Live strict CI evidence

Latest strict verification: GitHub Actions `Solver Verify` run **#42**
(`run_id=34070493121`), artifact id `10000303579`, digest
`sha256:a96eecb6fcb7ad80ec242395993a43a0d47fba91b0b19aaf31b3f1f583a73c38`.

Pinned dependency heads in that run:

- core `b8c05dff14da0b13608950a73906287dc0b601f9`
- scripts `49b0af044cebcb92f3f23211ef436971e1fc16fb`
- database `9d7f8da324417ec7913b47c52273906c9ae3333b`

### Tests

- **41/41 unit tests passed**.

### Conservative correctness probe

- 8/8 full games completed
- 2,983 engine decisions
- 2,983 filtered observation checks
- 2,983 filtered policy-prompt checks

### Paired stochastic adversarial coverage

- 8/8 full games completed
- 8,096 engine decisions
- 8,096 observation checks
- 8,096 policy-prompt checks
- decisions per game 551–1,437, mean 1,012
- process steps per game 1,164–2,939, mean 2,075.5

Decision families exercised:

- chain 5,535
- idle 1,545
- select place 361
- battle 316
- select card 199
- select/unselect 81
- tribute 39
- position 9
- option 4
- effect yes/no 4
- announce race 2
- select sum 1

These outcomes are engine/rules coverage only. Debug winners must not become deck
fitness or win-rate evidence.

### First zero-prior learning smoke

- 8/8 full training games completed
- 9,132 total engine decisions
- 8,634 decisions controlled by learned policy
- 498 decisions handled by legality fallback
- 1,463 nonzero sparse policy weights generated
- no external strategy priors
- policy `sparse_softmax_reinforce_v1`
- policy seed `20260906`

Learning decision coverage included chain, idle, battle, card/zone selection,
tribute, select/unselect, position, disable-field, yes/no, options, announce race
and select-sum.

This proves learning-data flow and complete-game self-play, **not pilot skill**.
There is not yet held-out evidence that the learned policy beats the zero-prior
baseline. The smoke policy is retained in run #42's artifact as
`reports/learned_policy_reborn_smoke.json`.

## Important invalidated run

Run **#31** (`run_id=34069085396`) must not be reused as successful evidence.
Although the workflow job was green, both stochastic and learning reports showed
0/8 completed with:

`UnsupportedInteraction: pilot observation belongs to wrong player`

Cause: filtered prompt was passed where `StochasticLegalPilot` expected the
viewer observation; the same bug existed in `LearningPilot` fallback.

Fixes now live:

- stochastic probe passes observation correctly;
- learning fallback passes observation correctly;
- regression coverage added;
- conservative/stochastic/learning probes now exit nonzero if all requested games
  do not complete;
- learner also requires nonzero learned decisions and weights.

Run #42 is the strict post-fix replacement.

## Current engine/text coverage audit

Run #42 engine coverage:

- 2,251 / 2,273 Reborn titles mapped to engine IDs
- 22 engine-name mismatches remain
- 1,880 mapped Lua scripts
- 370 scriptless Normal Monsters
- 1 mapped effect card with strict missing-script path:
  `Red-Eyes Darkness Metal Dragon`
- 1,938 exact official/engine text matches
- 313 non-exact comparisons still requiring audit

Historical official-page reconciliation from the earlier checkpoint also had 32
name mismatches to review. Do not silently resolve any of these using historical
scripts or approximate names.

## Durable duel observations

Read `reports/DUEL_OBSERVATIONS.md` before continuing. It records the exact
verified coverage counts, invalidated-run history, rules profile, engine audit and
evidence boundaries.

## Evidence boundary

There is still:

- **no certified deck win-rate table**;
- **no validated strongest pilot**;
- **no certified payoff matrix**;
- **no #1 deck**;
- **no global optimality claim**.

The current successful games demonstrate that generated Reborn decks can now be
piloted through complete legal games with privacy-safe observations and that a
zero-prior policy can update from outcomes. They do not identify the best deck.

## Resume from here

1. Build frozen-policy **held-out pilot evaluation**. Train on one seed set,
   freeze the policy, then compare it against the zero-prior stochastic baseline
   on disjoint seeds with seats paired/swapped. Keep this strictly a pilot-skill
   experiment, not deck ranking.
2. If the learned pilot does not improve on held-out play, improve the learning
   algorithm/state representation rather than using its deck results.
3. Add safe action enumeration/learning for the 498 complex fallback decisions
   observed in the smoke run (multi-card, tribute/sum/counter/place families as
   applicable).
4. Reconcile 22 engine-name mismatches, 313 text differences and the strict
   Red-Eyes Darkness Metal Dragon latest-errata script blocker.
5. Add Extra Deck construction/optimization as a search dimension.
6. Only after a pilot has held-out skill evidence and relevant card/effect audits
   are sufficiently covered, generate paired-seat certified payoff data and feed
   it to adversarial best-response / double-oracle deck search.
7. Continue full-pool candidate generation without human archetype/meta priors;
   use engine-certified results only when the corresponding evidence gates are
   satisfied.
